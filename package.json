{
  "name": "freeagentsltd-monorepo",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@8.6.12",
  "workspaces": [
    "apps/web",
    "apps/bot-worker",
    "packages/shared"
  ],
  "scripts": {
    "dev": "pnpm --filter web dev & pnpm --filter bot-worker dev",
    "build": "pnpm --filter shared build && pnpm --filter web build && pnpm --filter bot-worker build",
    "install:all": "pnpm install --recursive",
    "clean": "rm -rf node_modules && rm -rf apps/web/node_modules && rm -rf apps/bot-worker/node_modules && rm -rf packages/shared/dist"
  },
  "devDependencies": {
    "turbo": "^1.10.6"
  }
}