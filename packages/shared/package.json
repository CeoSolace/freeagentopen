{
  "name": "@freeagentsltd/shared",
  "version": "1.0.0",
  "description": "Shared constants, types, schemas and helpers for the FreeAgentsLTD platform",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^3.22.2"
  }
}